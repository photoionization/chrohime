name: RadioButton
type: refcounted
inherit: Checkbox
description: A native themed class representing a radio button.

constructors:
  - name: Create
    description: Create a radio button.

  - name: CreateWithTitleAndGroup
    description: Create a radio button with `title` and `group` id.
    args:
      - name: title
        type: string
      - name: group
        type: int
