name: Link
type: refcounted
inherit: Label
description: A Link is a label subclass that looks like an HTML link.

constructors:
  - name: Create
    description: Create a new empty Link.

events:
  - name: onClick
    description: Emitted when link is clicked.
    args:
      - name: link
        type: Link
